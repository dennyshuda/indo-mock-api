{
	"name": "indo-mock-api",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"test": "bun test",
		"start": "bun src/index.ts",
		"build": "bun build ./src/index.ts --outdir ./dist --target bun",
		"deploy": "wrangler deploy src/index.ts --name indo-mock-api"
	},
	"dependencies": {
		"@hono/swagger-ui": "^0.5.3",
		"hono": "^4.12.0"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"peerDependencies": {
		"typescript": "^5"
	}
}
